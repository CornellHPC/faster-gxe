[project]
name = "faster-gxe"
version = "0.1.0"
description = "GPU benchmark for randomized GxE trace estimators"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jax[cuda13]>=0.11.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
